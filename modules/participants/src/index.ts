/**
 * The participants module's public surface: the screen the application mounts, and
 * nothing else. The register and the domain types behind it stay inside the module.
 */

export { ParticipantsScreen } from "./ParticipantsScreen"
