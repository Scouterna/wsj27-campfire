/**
 * The participants module's public surface: the route table the application mounts,
 * and the section label the application's menu shares with the screen. The list of
 * participants and the domain types behind it stay inside the module.
 */

export { participantsSectionLabel } from "./ParticipantsScreen"
export { participantsRoutes } from "./routes"
