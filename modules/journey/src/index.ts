/**
 * The journey module's public surface – the widget table the application merges, with
 * the countdown a screen places by id. The dates, the phases, and the counts stay inside,
 * because everything the widget shows is derived from the fixed itinerary.
 */

export { journeyWidgets } from "./widgets"
