#!/bin/sh
# Holds the commit message to the Commit messages section of AGENTS.md.
#
# Fails the commit without pnpm, because someone who cannot run it is not set up to
# commit here.

pnpm exec commitlint --config config/commitlint/commitlint.config.ts --edit "$1"
