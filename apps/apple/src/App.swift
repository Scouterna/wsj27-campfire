import SwiftUI

/// The shell. One screen, so that the project, the schemes, and the toolchain have
/// something to build and run.
@main
struct CampfireApp: App {
  var body: some Scene {
    WindowGroup {
      Text("app.name")
    }
  }
}
