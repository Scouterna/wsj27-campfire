#include "config/Shared.xcconfig"

// The web application on this machine – the one origin every environment serves, so this
// address is right whichever stack is running.
//
// `localhost` rather than the loopback address on purpose: a session's cookies do not
// cross between spellings of the same machine.
CAMPFIRE_WEB_ORIGIN = http:/$()/localhost:8000
