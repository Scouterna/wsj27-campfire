# The JVM the Gradle daemon runs on, pinned here rather than left to whatever `java` the
# developer's PATH happens to find. 25 is the runtime Android Studio bundles, so the
# JDK the IDE builds with and the JDK the command line builds with are the same one and
# nobody has to install a second.
toolchainVersion=25
