# The JVM the Gradle daemon runs on, pinned rather than left to whatever `java` is on the
# PATH. It is the runtime Android Studio bundles, so the IDE and the command line build
# with the same JDK and nobody installs a second.
toolchainVersion=25
